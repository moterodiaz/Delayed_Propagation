# Decisions Log

Format: `[YYYY-MM-DD] DECISION: ... | REASONING: ... | CONTEXT: ...`

[2026-05-30] DECISION: Backend "brain" (cost, detection, options, network gap, grounded chatbot) ported to TypeScript Next.js API routes (`/api/state`, `/api/chat`) as the live path; the original Python FastAPI backend (`backend/`) is kept as a reference only. | REASONING: Team frontend is Next.js/TS; one app deploys cleanly with no second server / CORS. | CONTEXT: Python backend was built and pushed first (commit 3da1d9b) before discovering the repo had become a Next.js app.

[2026-05-30] DECISION: Cost model rebuilt to maximize the DEFENSIBLE total disruption cost using published per-unit rates, not hand-picked figures. Total rose from ~$80.6k to ~$351.3k. | REASONING: We pitch a cost optimizer — the bigger the defensible loss, the bigger the value of optimizing it. Every line item now cites a public source so the number survives scrutiny. | CONTEXT: Rates: A4A 2024 $100.76/block-min; FAA/DOT $0.78/min/pax × 150 seats; reaccommodation ~$150/pax (Hawaiian A330 precedent); diversion penalty $92.5k (EUROCONTROL / TPG $25k–$200k+ range); crew overnight $1.8k. Full methodology in Context(Docs)/Cost_Model.md.

[2026-05-30] DECISION: Fixed the hero (JBU1575) showing $0 cost. Disruption is now modeled as wasted block TIME (whole flight returned to origin) + diversion penalty, not "extra distance vs direct route". | REASONING: The old model computed extra_nm = flown − directToKingston, which went negative (it U-turned before reaching Kingston) and clamped to 0. Block-time + turnback is both bigger and more accurate — the flight accomplished zero net progress and still owes the Kingston leg. | CONTEXT: Hero now contributes block time + the $92.5k diversion penalty.

[2026-05-30] DECISION: Do NOT itemize a separate fuel cost line. | REASONING: A4A's $100.76/block-min already includes fuel ($33.06/min). Teammate's Chatbot.md added fuel separately (373 nm × 1.8 gal/nm × $3.50 = $2,350) which would double-count against a block-minute model. | CONTEXT: The 373 nm wasted is real (PROJECT_CONTEXT.md §4) and is preserved as narrative in the cost `detail` string, not summed into the total. Retires the earlier BURN_GAL_PER_NM=12.0 unit error.

[2026-05-30] DECISION: Exposed the cost methodology as data — `GET /api/state` now returns `costModel[]` (every rate + unit + source), plus per-event `cost.lineItems[]` with a source string per line. | REASONING: User wants "a place to show the data." Frontend can render a methodology/breakdown panel directly from the API; nothing is hidden in code comments. | CONTEXT: See Context(Docs)/Cost_Model.md for the human-readable version.

[2026-05-30] DECISION: Reviewed teammate's Chatbot.md numbers via a dedicated analysis agent before changing anything. Verdict: 373 nm wasted = real ADS-B (kept); $2,350 fuel = correct math on an unsourced 1.8 gal/nm assumption (superseded, see fuel decision); $41,200 network total, ~$8k divert, ~$10k pre-empt = placeholders (superseded by computed figures). | REASONING: Don't discard a teammate's work blindly; preserve what's grounded (the real distance), replace what's placeholder with sourced computation. | CONTEXT: OPEN ITEM — Chatbot.md and our `app/api/chat/route.ts` both target the chatbot; numbers must come from `/api/state` (computed), not from the hardcoded system prompt in Chatbot.md, or grounding breaks.
