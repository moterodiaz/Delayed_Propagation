// Root landing = the ops cost-optimizer dashboard (pricing + priced options + live chatbot).
// The earlier simulation/timeline view is preserved at /timeline.
export { default } from "./demo/page";
